import type { Topic } from "../types";

const baseMistakes = [
  "只背名词，不说明输入、输出、限制条件。",
  "把训练阶段可并行和推理阶段逐 token decode 混在一起。",
  "公式写对但不解释每一项的含义。",
];

export const topics: Topic[] = [
  {
    id: "evaluation",
    navTitle: "Evaluation Metrics",
    title: "评估指标 (evaluation metrics)",
    subtitle: "分类、语言模型与生成任务的考试公式区。",
    priority: "low",
    tags: ["Accuracy", "Precision", "Recall", "F1", "BLEU", "Perplexity"],
    concepts: ["Confusion matrix: TP, FP, FN, TN。", "Accuracy = (TP + TN) / total，但类别极不平衡时会误导。", "990 oranges / 10 apples 且全预测 oranges 时，accuracy = 99%，但 apple recall = 0。", "Precision 关心 false positives，Recall 关心 false negatives，F1 是调和平均。"],
    importance: ["Final 低优先级但公式必会。", "分类、翻译、语言模型 evaluation 经常作为短答或计算题。"],
    formulas: ["Accuracy = (TP + TN) / (TP + FP + FN + TN)", "Precision = TP / (TP + FP)", "Recall = TP / (TP + FN)", "F1 = 2PR / (P + R)", "PPL = exp(-1/N sum_t log P(w_t | w_<t))"],
    intuition: ["Precision 像问：我报出的正例有多少是真的？Recall 像问：真实正例我抓到了多少？", "F1 不是算术平均；当一个指标很低时，F1 会被明显拉低。"],
    examQuestions: ["什么时候 precision 更重要？", "什么时候 recall 更重要？", "为什么 accuracy 在 imbalanced data 下可能误导？", "BLEU 的局限是什么？"],
    answerTemplate: "先画 confusion matrix，再写公式。若类别不平衡，说明 accuracy 可能高但 minority class recall 很低；precision 主要惩罚 FP，recall 主要惩罚 FN，F1 用调和平均平衡二者。",
    commonMistakes: ["把 FP 和 FN 位置写反。", "说 F1 是 precision 和 recall 的算术平均。", "只报 accuracy，不检查 minority class。"],
    miniQuiz: ["TP=10, FP=5, FN=5, TN=80 时 F1 是多少？", "如果模型没有预测任何正例，precision 如何处理？", "BLEU 是否能可靠判断语义等价 paraphrase？"],
    playgroundIds: ["confusion", "loss"],
  },
  {
    id: "cuda",
    navTitle: "CUDA / GPU",
    title: "CUDA / GPU 编程 (GPU programming)",
    subtitle: "代码阅读、SIMT、warp divergence、coalescing、shared memory bank conflict。",
    priority: "high",
    tags: ["CUDA", "SIMT", "Warp", "Coalescing", "Bank conflict"],
    concepts: ["CPU: fewer powerful cores, cache/control heavy。GPU: many simple cores, throughput-oriented。", "GPGPU 是用 GPU 执行通用计算。", "CUDA flow: host prepares data -> cudaMalloc -> cudaMemcpy H2D -> kernel<<<blocks, threads>>> -> cudaDeviceSynchronize -> cudaMemcpy D2H -> cudaFree。", "__global__ 从 host 调用在 device 运行；__device__ 在 device 调用运行；__host__ 在 host 运行。", "Thread hierarchy: grid -> blocks -> threads；warp 通常 32 threads。", "SIMT (single instruction, multiple threads) 中同一 warp 线程通常执行同一指令。"],
    importance: ["教授 final topic 高优先级，最可能出现 CUDA snippet reading。", "能否从代码识别 index、bounds、memory pattern、divergence、sync 与 bank conflict，是硬件基础核心能力。"],
    formulas: ["1D: i = blockIdx.x * blockDim.x + threadIdx.x", "Bounds: if (i < N)", "2D: col = blockIdx.x * blockDim.x + threadIdx.x; row = blockIdx.y * blockDim.y + threadIdx.y; idx = row * width + col", "Row-major: Image[j][i] = Image[j * width + i]", "Bank conflict under 32 banks: gcd(stride, 32)"],
    intuition: ["GPU 要让相邻线程做相似工作、访问相邻地址。", "Warp divergence 只在同一 warp 内是问题；不同 warp 走不同分支不叫同一 warp divergence。", "Row-wise image access usually coalesced；column-wise access often strided。", "Shared memory 是 block-local low-latency memory；__syncthreads 只同步同一个 block。"],
    examQuestions: ["给 kernel 判断 global index 和 bounds check。", "判断 A[i] 与 A[i * stride] 哪个更 coalesced。", "if (threadIdx.x % 2 == 0) 是否造成 warp divergence？", "shared[threadIdx.x * 32] 在 32 banks 下几路冲突？", "tile[32][32] vs tile[32][33] column access 为什么 padding 有用？"],
    answerTemplate: "First compute the global index. Then check bounds. Next inspect whether consecutive threads access consecutive memory. Then check whether branches differ within a warp. If shared memory is used, check whether synchronization is needed and whether stride causes bank conflicts.",
    commonMistakes: ["把 block divergence 误说成 warp divergence。", "忘记 edge blocks 需要 if (i < N)。", "认为所有 shared memory 访问都更快，忽略 bank conflict 和 __syncthreads。", "把 AoS/SoA 对 CPU/GPU 的偏好说反。"],
    miniQuiz: ["blockIdx.x=6, blockDim.x=4, threadIdx.x=1 时 global index 是多少？", "stride=16 的 shared memory access 是几路 bank conflict？", "if (threadIdx.x < 32) 在 64-thread block 中是否造成同一 warp 分歧？"],
    playgroundIds: ["cudaIndex", "warp", "coalescing", "bank"],
  },
  {
    id: "nlp",
    navTitle: "NLP Foundations",
    title: "NLP 基础与语言模型 (language models)",
    subtitle: "Representation + modeling，tokenization，TF-IDF，chain rule。",
    priority: "medium",
    tags: ["NLP", "Embeddings", "TF-IDF", "BPE", "Language model"],
    concepts: ["NLP tasks: text classification, machine translation, speech recognition, information extraction, QA, text generation。", "两根支柱：representation and modeling。", "Word indexing 是任意整数 ID，不表达语义相似度。", "One-hot sparse/high-dimensional，所有词距离类似。", "Word embeddings 是 dense vectors，由 embedding matrix 学习语义相似性。", "Contextual embeddings 允许同一个词在不同上下文有不同 vector。", "BoW counts words and ignores order。", "BPE 从 characters/bytes 开始，重复合并 frequent pairs，缓解 OOV 与 vocabulary size。"],
    importance: ["Transformer 之前的表示方法常用于概念对比题。", "语言模型 chain rule 是 next-token prediction 的数学基础。"],
    formulas: ["TF-IDF(w,d) = TF(w,d) * log(N / DF(w))", "P(w1,...,wn) = product_t P(w_t | w_<t)", "Next token: P(w_t | w_1,...,w_{t-1})", "cos(A,B) = (A · B) / (||A|| ||B||)"],
    intuition: ["TF-IDF 提高某文档中特别常见、但整个语料不普遍的词。", "Chain rule 把整句概率拆成逐 token 条件概率。"],
    examQuestions: ["为什么 integer word IDs 不代表语义相似？", "BoW 丢失什么信息？", "BPE 为什么能处理 OOV？", "写出 P(I, am, happy) 的 chain rule。"],
    answerTemplate: "NLP 模型先把离散文本变成向量表示，再用模型学习序列或任务结构。Word embeddings 比 one-hot 更 dense 且能表达语义；语言模型用 chain rule 做 next-token prediction。",
    commonMistakes: ["认为词 ID 大小有语义顺序。", "把 TF-IDF 当成上下文 embedding。", "忘记 BoW ignores order。"],
    miniQuiz: ["P(I, am, happy) 如何分解？", "TF-IDF 为什么 downweight common words？", "cosine similarity = -1 表示什么？"],
    playgroundIds: ["tfidf", "cosine"],
  },
  {
    id: "rnn-lstm",
    navTitle: "RNN / LSTM",
    title: "RNN / LSTM",
    subtitle: "序列建模、BPTT、vanishing/exploding gradients、gates。",
    priority: "high",
    tags: ["RNN", "LSTM", "BPTT", "Vanishing gradients"],
    concepts: ["RNN handles sequences and variable-length input。", "Same weights shared across time。", "h_t acts as memory。", "Backpropagation through time (BPTT) 会反复乘 recurrent weights。", "LSTM 用 cell state C_t 作为 long-term memory，用 hidden state H_t 作为 short-term exposed state。", "Gates: forget gate, input gate, output gate。"],
    importance: ["Final high priority includes RNN / LSTM / Seq2Seq。", "RNN 的梯度问题解释了 LSTM 和 attention/Transformer 的动机。"],
    formulas: ["h_t = f(Ux_t + Wh_{t-1} + b_h)", "o_t = g(Vh_t + b_y)", "W=2 over 100 steps -> 2^100 amplification; W=0.5 -> 2^-100 shrinkage", "C_t = f_t ⊙ C_{t-1} + i_t ⊙ C~_t", "H_t = o_t ⊙ tanh(C_t)"],
    intuition: ["Vanilla RNN 像一条很长的乘法链，数值会越来越大或越来越小。", "LSTM 的 gates 像可学习阀门，决定忘多少、写多少、暴露多少。"],
    examQuestions: ["为什么 RNN 可以处理 variable-length input？", "为什么 BPTT 会导致 vanishing/exploding gradients？", "LSTM 的 C_t 和 H_t 有什么区别？", "三个 gate 各自作用是什么？"],
    answerTemplate: "RNNs share recurrent weights across time. During backpropagation through time, gradients are repeatedly multiplied by the recurrent matrix, causing vanishing or exploding gradients for long sequences. LSTM uses gates and a cell state to preserve useful long-term information.",
    commonMistakes: ["说 LSTM 完全消除所有梯度问题。", "混淆 C_t 和 H_t。", "忘记 RNN 参数跨时间共享。"],
    miniQuiz: ["W=0.5, T=20 时梯度倾向 vanish 还是 explode？", "forget gate 值接近 0 表示什么？", "output gate 控制什么？"],
    playgroundIds: ["rnn", "lstm"],
  },
  {
    id: "seq2seq",
    navTitle: "Seq2Seq & Attention",
    title: "Seq2Seq 编码器-解码器与交叉注意力 (cross-attention)",
    subtitle: "Encoder, decoder, context bottleneck, teacher forcing, attention alignment。",
    priority: "high",
    tags: ["Seq2Seq", "Cross-attention", "Teacher forcing"],
    concepts: ["Encoder reads source sequence；decoder generates target sequence。", "固定 context vector 会成为长句 bottleneck。", "Teacher forcing: training 中 feed true previous target token，而不是模型上一步预测。", "Cross-attention: decoder 当前 state 对 encoder hidden states 动态加权。"],
    importance: ["Cross-attention 是 encoder-decoder Transformer 的前身概念。", "注意力热力图和 teacher forcing 是常见短答题。"],
    formulas: ["e_{t,i} = a(s_{t-1}, h_i)", "alpha_{t,i} = exp(e_{t,i}) / sum_k exp(e_{t,k})", "c_t = sum_i alpha_{t,i} h_i", "s_t = f(s_{t-1}, y_{t-1}, c_t)"],
    intuition: ["Decoder 每生成一个词，都重新问：source sentence 哪些位置现在最相关？", "Teacher forcing 防止训练早期错误滚雪球。"],
    examQuestions: ["Seq2Seq 的 fixed context vector bottleneck 是什么？", "Cross-attention 如何缓解 bottleneck？", "Teacher forcing 中如果模型预测错了，下一步训练输入是什么？"],
    answerTemplate: "Cross-attention avoids compressing the entire source sentence into one vector by dynamically weighting encoder hidden states at each decoder step. Teacher forcing feeds the true previous target token during training.",
    commonMistakes: ["把 self-attention 和 cross-attention 混同。", "认为 teacher forcing 在 inference 也可用。", "忘记 attention weights 需要 softmax 归一化。"],
    miniQuiz: ["alpha_{t,i} 的和是多少？", "Decoder query 来自哪里？", "Encoder hidden states 在 cross-attention 中扮演 K/V 还是 Q？"],
    playgroundIds: ["seq2seq"],
  },
  {
    id: "transformer-arch",
    navTitle: "Transformer Architecture",
    title: "Transformer 架构 (Transformer architecture)",
    subtitle: "Encoder-only, decoder-only, encoder-decoder, Add & Norm。",
    priority: "high",
    tags: ["Transformer", "BERT", "GPT", "Add & Norm"],
    concepts: ["Transformer removes recurrence and convolution, uses attention。", "Encoder block: multi-head self-attention -> add & norm -> feed-forward -> add & norm。", "Decoder block: masked self-attention -> cross-attention -> feed-forward，每段后有 add & norm。", "Decoder-only block: masked self-attention + feed-forward。", "Encoder-only: BERT, understanding, bidirectional。Decoder-only: GPT, generation, causal。Encoder-decoder: translation/summarization。", "Residual connections improve gradient flow；LayerNorm stabilizes activations。"],
    importance: ["Final high priority: architecture, causal attention, MHA, positional encoding。", "能区分 BERT/GPT/encoder-decoder 是基础判断题。"],
    formulas: ["Block output roughly: LayerNorm(x + Sublayer(x))", "Decoder-only LM predicts P(w_t | w_<t)", "Encoder-only can attend bidirectionally unless masked by task."],
    intuition: ["Encoder 像读完整文本做理解；decoder-only 像从左到右写文本；encoder-decoder 先读源语言再写目标语言。"],
    examQuestions: ["BERT 为什么不是 causal generation model？", "GPT 为什么需要 masked self-attention？", "Add & Norm 两部分分别有什么作用？"],
    answerTemplate: "Transformer replaces recurrence with attention. Encoder-only models are bidirectional for understanding, decoder-only models use causal masked self-attention for generation, and encoder-decoder models use cross-attention for conditional generation.",
    commonMistakes: ["说所有 Transformer 都能看未来。", "忽略 decoder cross-attention 只在 encoder-decoder 中出现。", "把 LayerNorm 说成 dropout。"],
    miniQuiz: ["GPT 属于哪类架构？", "Translation 更常用哪类架构？", "Residual connection 对梯度有什么帮助？"],
    playgroundIds: ["mha", "mask"],
  },
  {
    id: "position-cosine",
    navTitle: "Position & Cosine",
    title: "位置编码与余弦相似度 (positional encoding & cosine similarity)",
    subtitle: "Self-attention 的顺序信息、RoPE、embedding direction。",
    priority: "high",
    tags: ["Positional encoding", "RoPE", "Cosine"],
    concepts: ["Self-attention alone is permutation-invariant。", "Without position, dog chased cat 与 cat chased dog 可能看起来相似。", "Input = token embedding + positional encoding。", "Sinusoidal PE 使用多频 sin/cos，形成 Fourier-like fingerprint。", "Learned positional embedding 从数据学习位置向量。", "Relative positional encoding 直接建模相对距离。", "RoPE rotates Q and K based on position before dot-product，使 attention 依赖 relative distance。", "Cosine similarity 比较方向：1 same direction, 0 orthogonal, -1 opposite。"],
    importance: ["位置编码是 Transformer 高优先级概念。", "Cosine similarity 常与 embeddings 一起考计算和直觉。"],
    formulas: ["PE(pos, 2i) = sin(pos / 10000^(2i / d_model))", "PE(pos, 2i+1) = cos(pos / 10000^(2i / d_model))", "cos(A,B) = (A · B) / (||A|| ||B||)"],
    intuition: ["不同频率让每个位置有独特但可泛化的签名。", "RoPE 不是把位置简单相加，而是旋转 Q/K 后再 dot-product。"],
    examQuestions: ["为什么 self-attention 需要 position？", "PE(pos, i) 是 scalar 还是 vector component？", "RoPE 高层思想是什么？", "为什么 embedding 常用 cosine similarity？"],
    answerTemplate: "Self-attention does not know token order by itself, so token embeddings are combined with positional information. Sinusoidal PE uses multiple sine/cosine frequencies; RoPE encodes position by rotating Q/K so dot-products reflect relative distance.",
    commonMistakes: ["说 PE 是一个单独 scalar。", "把 RoPE 说成 learned absolute embedding。", "把 cosine 当成欧氏距离。"],
    miniQuiz: ["PE 的 pos 和 i 分别代表什么？", "cosine = 0 表示什么？", "位置向量通常在输入哪一步加入？"],
    playgroundIds: ["position", "cosine"],
  },
  {
    id: "attention",
    navTitle: "Self-Attention",
    title: "自注意力 (self-attention) 与多头注意力 (multi-head attention)",
    subtitle: "Q/K/V, scaled dot-product, shapes, causal mask。",
    priority: "high",
    tags: ["QKV", "Self-attention", "Masked attention", "MHA"],
    concepts: ["Given X in R^{n x d_model}: Q=XW_Q, K=XW_K, V=XW_V。", "Q asks what this token needs；K describes what each token offers；V contains information to be mixed。", "QK^T gives token-token scores。", "Divide by sqrt(d_k) prevents very large logits and softmax saturation。", "softmax gives attention weights；multiply by V gives weighted sum。", "Decoder causal mask sets future logits to -infinity before softmax；token i can attend only to j <= i。", "MHA lets heads learn different subspaces/relations。"],
    importance: ["Final 最高优先级区域之一。", "Shape calculation、mask matrix、QKV step 是常见考试题。"],
    formulas: ["Q: n x d_k; K: n x d_k; V: n x d_v", "QK^T: n x n", "Attention weights: n x n", "Output: n x d_v", "Attention(Q,K,V)=softmax(QK^T/sqrt(d_k))V", "head_i = Attention(QW_Q^i, KW_K^i, VW_V^i)"],
    intuition: ["每个 token 用自己的 Q 去和所有 K 匹配，然后从 V 中混合上下文。", "Causal mask 让训练能并行处理完整 target，同时保持 autoregressive causality。"],
    examQuestions: ["如果 X 是 n x d_model，QK^T shape 是什么？", "为什么要除以 sqrt(d_k)？", "Masked attention 为什么训练时也需要？", "MHA 比单头多了什么能力？"],
    answerTemplate: "Self-attention computes pairwise token relevance inside the same sequence. QK^T gives similarity scores, softmax converts them to weights, and multiplying by V produces contextualized token representations. Masked attention blocks future tokens for causal generation.",
    commonMistakes: ["把 QK^T shape 写成 d_k x d_k。", "说 masked attention 是为了省内存而不是防止看未来。", "认为多头只是重复同一 attention。"],
    miniQuiz: ["n=8, d_k=16 时 QK^T shape？", "Future logits 被设成什么？", "B=1 的 beam search 和什么等价？"],
    playgroundIds: ["shape", "mask", "mha"],
  },
  {
    id: "training",
    navTitle: "Training & Evaluation",
    title: "Transformer 训练与评估 (training & evaluation)",
    subtitle: "Data, tokenization, cross-entropy, shifted targets, PPL, BLEU。",
    priority: "high",
    tags: ["Training", "Cross entropy", "Perplexity", "BLEU"],
    concepts: ["Training flow: data collection -> tokenization -> embeddings + positional encodings -> forward pass -> cross-entropy loss -> backpropagation。", "Pretraining 学 broad next-token/statistical patterns；fine-tuning 适配任务或风格。", "Correct high probability -> low loss；correct low probability -> high loss。", "Training decoder can be parallel because shifted target + causal mask。", "Evaluation: F1 for classification, perplexity for language models, BLEU for translation/generation。", "BLEU uses n-gram overlap and brevity penalty, but weak on semantics/paraphrases。"],
    importance: ["trainingTransformers PDF 中 evaluation、decoding、RLHF 都是 final 重点。", "Loss 与 PPL 计算经常出现在公式题。"],
    formulas: ["Loss = -sum_i y_i log(p_i)", "One-hot: Loss = -log(p_correct)", "Sequence loss = -1/N sum_j sum_i y_{j,i} log(p_{j,i})", "PPL = exp(-1/N sum_t log P(w_t | w_<t))"],
    intuition: ["Cross-entropy 是惩罚模型对正确 token 不自信。", "PPL=10 可理解为平均像在 10 个等可能选项中选择。"],
    examQuestions: ["为什么 decoder training 可以并行？", "PPL 越低越好吗？完美模型 PPL 接近多少？", "BLEU 的局限是什么？"],
    answerTemplate: "Transformer training uses tokenized sequences, shifted targets, causal masks, cross-entropy loss, and backpropagation. Evaluation depends on task: F1 for classification, perplexity for language modeling, and BLEU for n-gram translation overlap.",
    commonMistakes: ["把 inference decode 的逐 token 性质套到 training。", "认为 PPL 越高越好。", "说 BLEU 能完整判断语义质量。"],
    miniQuiz: ["p_correct=0.8 时 CE loss 是 -log(0.8) 还是 -log(0.2)？", "随机模型 PPL 大约接近什么？", "BLEU 是否有 brevity penalty？"],
    playgroundIds: ["loss"],
  },
  {
    id: "kv-cache",
    navTitle: "KV Cache",
    title: "推理：KV Cache、Prefill vs Decode",
    subtitle: "Autoregressive inference 的计算复用与内存 trade-off。",
    priority: "high",
    tags: ["KV cache", "Prefill", "Decode", "Inference"],
    concepts: ["Autoregressive inference generates one token at a time。", "Without cache, model recomputes growing sequence every step。", "K and V for previous tokens do not change。", "Cache K/V for each previous token and layer。", "Why cache K/V, not old Q? Old K/V are reused by future queries；Q only needed for current token。", "Prefill processes full prompt in parallel and builds initial KV cache。Decode generates one token at a time, uses cached K/V, appends new K/V。"],
    importance: ["Final 明确高优先级：KV cache、prefill vs decode、why KV cache matters。", "这是 Transformer 推理硬件效率的核心。"],
    formulas: ["KV memory ~= 2 * layers * heads * head_dim * sequence_length * bytes_per_value", "Optimized decode: compute new Q/K/V -> retrieve cached K/V -> concat -> current Q attends all K -> weighted sum V -> append K/V"],
    intuition: ["未来每个新 token 都要看旧 token 的 K/V，所以旧 K/V 值得保存。旧 Q 不会被未来再次查询。", "KV cache 用 memory 换 compute 和 latency。"],
    examQuestions: ["为什么 cache K/V 而不是 old Q？", "Prefill 和 decode 各做什么？", "KV cache 的 memory 随哪些因素增长？", "没有 cache 时重复计算在哪里？"],
    answerTemplate: "Prefill processes the full prompt in parallel and creates the KV cache. Decode generates one token at a time, reusing cached K/V and computing only the new token’s Q/K/V.",
    commonMistakes: ["说 KV cache 减少所有 attention 计算到 O(1)。", "忘记 KV cache 每层都存。", "把 prefill 和 decode 顺序说反。"],
    miniQuiz: ["layers=2, heads=4, head_dim=8, seq=10, bytes=2 时 KV memory 多少 bytes？", "Decode 阶段一次生成几个 token？", "旧 K/V 会随新 query 改变吗？"],
    playgroundIds: ["kv"],
  },
  {
    id: "flash",
    navTitle: "FlashAttention",
    title: "FlashAttention 高层思想 (IO-aware exact attention)",
    subtitle: "Exact attention optimized for GPU memory hierarchy。",
    priority: "high",
    tags: ["FlashAttention", "HBM", "SRAM", "Tiling"],
    concepts: ["Standard attention materializes n x n attention matrix。", "Long sequences make this matrix memory expensive。", "FlashAttention computes exact attention more efficiently；it is not approximate attention。", "It is IO-aware and uses tiling/blocking to reduce reads/writes between GPU HBM and on-chip SRAM/shared memory。", "It avoids storing the full attention matrix in HBM。"],
    importance: ["Final high priority includes FlashAttention basic idea。", "考试只需高层 IO-aware exact attention，不需要复杂推导。"],
    formulas: ["Same mathematical attention result: softmax(QK^T/sqrt(d_k))V", "Optimization target: fewer HBM <-> SRAM transfers, no full n x n matrix in HBM."],
    intuition: ["不是换了 attention 公式，而是换了 GPU 上的计算/存储顺序。", "像用 tile 在片上 SRAM 中完成局部计算，减少昂贵 HBM 往返。"],
    examQuestions: ["FlashAttention 是 approximate attention 吗？", "它优化的是 compute 公式还是 memory IO？", "为什么长序列下 n x n matrix 很贵？"],
    answerTemplate: "FlashAttention is exact attention optimized for GPU memory hierarchy. It tiles Q/K/V and avoids materializing the full attention matrix in slow HBM.",
    commonMistakes: ["把 FlashAttention 说成低秩或稀疏 approximate attention。", "只说 faster，不提 IO-aware/HBM/SRAM。", "认为它改变模型输出含义。"],
    miniQuiz: ["标准 attention 物化什么大矩阵？", "FlashAttention 是否 exact？", "HBM 与 SRAM 哪个更靠近 compute？"],
    playgroundIds: ["flash"],
  },
  {
    id: "decoding",
    navTitle: "Decoding",
    title: "解码策略 (decoding strategies)",
    subtitle: "Greedy, beam, temperature, top-k, top-p, contrastive, speculative。",
    priority: "high",
    tags: ["Greedy", "Beam", "Temperature", "Top-k", "Top-p", "Speculative"],
    concepts: ["At each generation step, model outputs a probability distribution over vocabulary。", "Greedy picks argmax: fast, deterministic, can be repetitive/locally optimal。", "Beam search keeps B best partial sequences；B=1 equals greedy；better for translation, slower, not guaranteed global optimum。", "Temperature reshapes logits before softmax；T -> 0 greedy-like，T > 1 flatter/more random。", "Top-k keeps fixed k highest probability tokens。Top-p keeps smallest cumulative probability set >= p, adaptive。", "Contrastive decoding conceptually uses log p_expert(x) - alpha log p_base(x)。", "Speculative decoding: smaller draft model proposes multiple tokens; larger target model verifies in parallel。"],
    importance: ["trainingTransformers PDF 与 final topic explicitly list decoding strategies。", "比较题非常常见：greedy vs beam，top-k vs top-p，temperature effect，speculative why faster。"],
    formulas: ["P(x_i)=exp(z_i/T)/sum_j exp(z_j/T)", "Beam score often sums log probabilities。", "Contrastive concept: log p_expert(x) - alpha log p_base(x)"],
    intuition: ["Decoding 不是改变模型本身，而是改变如何从 distribution 选下一个 token。", "Top-p 比 top-k 更能适应分布尖锐或平坦的情况。"],
    examQuestions: ["B=1 的 beam search 等于什么？", "T<1 和 T>1 分别怎样改变分布？", "Top-k 与 top-p 的关键区别？", "Speculative decoding 为什么可能加速？"],
    answerTemplate: "Greedy keeps one local best path; beam keeps multiple candidates. Temperature reshapes logits before softmax. Top-k uses fixed candidate count; top-p uses adaptive cumulative probability threshold. Speculative decoding uses a small draft model and verifies with a large target model.",
    commonMistakes: ["说 beam search 保证全局最优。", "把 top-p 当成固定 p 个 token。", "认为 temperature 改变训练好的 logits 顺序以外的模型参数。"],
    miniQuiz: ["Top-p p=0.9 是保留 0.9 个 token 吗？", "Temperature 越高越确定还是越随机？", "Speculative decoding 的 target model 做什么？"],
    playgroundIds: ["decoding"],
  },
  {
    id: "rlhf",
    navTitle: "RLHF / Alignment",
    title: "RLHF / Post-training / Alignment",
    subtitle: "SFT, reward model, PPO/RL, KL penalty, helpfulness/harmlessness/honesty。",
    priority: "high",
    tags: ["RLHF", "SFT", "Reward model", "PPO", "Alignment"],
    concepts: ["Pretraining next-token prediction does not guarantee helpfulness or alignment。", "Post-training improves behavior for user intent。", "RLHF steps: SFT on human demonstrations；reward model from human preference rankings；RL fine-tuning often PPO with KL penalty。", "KL penalty avoids drifting too far from reference model。", "Concepts: helpfulness, harmlessness, honesty, alignment, reward hacking, labeler bias, reward overfitting。", "RLAIF / Constitutional AI: high-level alternative using AI feedback/principles。"],
    importance: ["Final high priority includes RLHF/post-training/alignment。", "常见题要求按步骤说明 pipeline 和风险。"],
    formulas: ["Preference data trains reward model r(x,y).", "RL objective conceptually maximizes reward while penalizing KL(policy || reference)."],
    intuition: ["Pretraining 学会续写互联网文本；RLHF 把输出偏向人类更喜欢、更安全、更有帮助的回复。", "Reward model 是代理目标，可能被 exploit，所以需要 KL 和评估。"],
    examQuestions: ["RLHF 三个主要阶段是什么？", "Reward model 的训练数据来自哪里？", "为什么需要 KL penalty？", "Reward hacking 是什么？"],
    answerTemplate: "RLHF aligns a pretrained language model with human preferences through supervised fine-tuning, reward modeling from rankings, and reinforcement learning with a KL penalty.",
    commonMistakes: ["把 reward model 当成最终聊天模型。", "漏掉 SFT 或 KL penalty。", "认为 RLHF 保证绝对正确或无害。"],
    miniQuiz: ["Human preference rankings 用来训练什么？", "PPO/RL 优化的是 policy 还是 tokenizer？", "Labeler bias 属于什么风险？"],
    playgroundIds: ["rlhf"],
  },
  {
    id: "quantization",
    navTitle: "Quantization",
    title: "量化与浮点表示 (quantization & floating point)",
    subtitle: "sign, exponent, mantissa; FP32/FP16/BF16/FP8/INT8/INT4。",
    priority: "high",
    tags: ["Floating point", "Quantization", "INT8", "INT4"],
    concepts: ["Floating point parts: sign, exponent, mantissa/fraction/significand。", "Exponent controls dynamic range；mantissa controls precision。", "FP32 more precise, more memory。FP16/BF16/FP8/INT8/INT4 lower memory, often faster, less precise。", "Quantization maps high-precision values to low-precision values。", "Benefits: smaller model, lower memory bandwidth, faster inference on supported hardware, lower KV/weight memory pressure depending on what is quantized。", "Trade-offs: quantization error, possible accuracy loss, outliers difficult, softmax/layer norm may need higher precision。"],
    importance: ["Final high priority includes quantization and floating point high-level idea。", "这是硬件效率、memory bandwidth、inference speed 的关键概念。"],
    formulas: ["q = round(x / scale) + zero_point", "x ~= scale * (q - zero_point)", "Memory roughly scales with bits per value."],
    intuition: ["用更少 bit 表示数，像把精细刻度换成粗刻度。", "省内存和带宽，但数值会被 round 到邻近可表示值。"],
    examQuestions: ["Exponent 和 mantissa 分别影响什么？", "INT4 相比 FP32 memory 约减少多少？", "为什么 outliers 对量化困难？", "哪些操作可能保留高精度？"],
    answerTemplate: "Quantization reduces numerical precision to save memory and speed up inference, but introduces approximation error. The trade-off is efficiency versus accuracy.",
    commonMistakes: ["认为量化一定无损。", "把 exponent 和 mantissa 作用说反。", "忽略硬件是否支持低精度加速。"],
    miniQuiz: ["FP32 到 INT8 理论上每值 memory 变为多少？", "zero_point 有什么作用？", "LayerNorm 为什么可能需要较高精度？"],
    playgroundIds: ["quant"],
  },
  {
    id: "templates",
    navTitle: "Answer Templates",
    title: "常见考试答题模板 (exam answer templates)",
    subtitle: "短答题可直接套用的结构。",
    priority: "high",
    tags: ["Templates", "Short answer"],
    concepts: ["SIMT: 一个 warp 中多个线程以 lockstep/same instruction 风格执行，但每个 thread 有自己的 register/threadIdx。", "Warp divergence: 同一 warp 内 branch condition 不同会串行执行分支路径。", "Coalescing: consecutive threads should access consecutive/global contiguous addresses。", "Bank conflict: 32 banks 下 stride 与 32 的 gcd 给出冲突路数，broadcast 例外。", "Masked attention: future logits -> -infinity, softmax 后 future weight -> 0。", "KV cache: reuse old K/V during decode; memory grows with layers/heads/head_dim/seq_len。"],
    importance: ["模板帮助把答案写成可评分结构。", "每个模板都先定义、再说明为什么重要、最后给典型后果。"],
    formulas: ["CUDA index -> bounds -> memory -> branch -> shared memory sync/banks", "Attention -> Q/K/V -> QK^T -> scale -> softmax -> V", "RLHF -> SFT -> reward model -> PPO/RL + KL"],
    intuition: ["考试答案不需要长，但要覆盖关键词和因果关系。"],
    examQuestions: ["解释 SIMT。", "解释 masked attention。", "解释 FlashAttention。", "解释 quantization trade-off。"],
    answerTemplate: "定义概念 -> 写关键公式或流程 -> 解释为什么重要 -> 点出常见 trade-off/风险。CUDA 题按 index/bounds/memory/divergence/shared memory 顺序检查。",
    commonMistakes: baseMistakes,
    miniQuiz: ["用一句话解释 KV cache。", "用一句话解释 FlashAttention。", "用一句话解释 RLHF。"],
  },
];

export const priorityItems = [
  ...["CUDA code snippet reading", "GPU architecture and execution model", "SIMT", "Warp divergence", "Coalesced memory access", "Shared memory bank conflicts", "RNN / LSTM / Seq2Seq", "Attention, cross-attention, self-attention", "Positional embeddings / positional encoding", "Transformer architecture", "Causal / masked attention", "Multi-head attention", "KV cache", "Prefill vs decode", "Why KV cache matters", "Transformer training", "Evaluation for language models", "FlashAttention basic idea", "Decoding: greedy, beam, temperature, top-k, top-p, contrastive, speculative", "RLHF / post-training / alignment", "Quantization and floating point high-level idea"].map((label) => ({ label, priority: "high" as const })),
  ...["NLP representation", "Word indexing / one-hot / embeddings", "BoW / TF-IDF", "Chain rule for language models", "Cosine similarity", "BLEU / perplexity / F1"].map((label) => ({ label, priority: "medium" as const })),
  { label: "Pre-midterm evaluation metrics formulas", priority: "low" as const },
];
